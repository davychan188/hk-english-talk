# Required for live AI chat (without this, the app runs in demo/mock mode)
OPENAI_API_KEY=sk-...

# Optional alias (either OPENAI_API_KEY or AI_API_KEY works)
# AI_API_KEY=sk-...

# Optional model override (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini
